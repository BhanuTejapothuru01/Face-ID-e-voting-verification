{
  "name": "facevote-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "start:backend": "cd backend && PYTHONPATH=. venv/bin/uvicorn app.main:app --port 8000 --reload",
    "build": "npm --prefix frontend run build",
    "install-all": "npm --prefix frontend install"
  }
}
